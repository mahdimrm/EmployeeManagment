@using Employee.Entities;
@model Grade

@{
    Layout = null;
}

<div class="content-body">
    <section>
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-content">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-12">
                                    <fieldset class="form-group">
                                        <label asp-for="Name"  class="control-label"></label>
                                        <input asp-for="Name" disabled type="text" class="form-control" />

                                    </fieldset>
                                </div>
                                <div class="col-md-12">
                                    <div class="form-group">
                                        <a href="@Url.Action("ConfirmationDelete","Grade",new{id=Model.Id})" class="btn btn-danger btn-block">حذف</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>
